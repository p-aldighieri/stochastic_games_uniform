# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: BREAKDOWN, PROVER, STOP_STALL
- Selected: BREAKDOWN
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: BREAKDOWN, PROVER, STOP_STALL
- Selected: BREAKDOWN
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: CONSOLIDATOR, PROVER
- Selected: CONSOLIDATOR
- Fallback: yes (router disabled)

# Router Decision

- Phase: consolidator
- Allowed: STOP_PASS
- Selected: STOP_PASS
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: BREAKDOWN, PROVER, STOP_STALL
- Selected: BREAKDOWN
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: STOP_FAIL_SCOPE
- Selected: STOP_FAIL_SCOPE
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: STOP_FAIL_SCOPE
- Selected: STOP_FAIL_SCOPE
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

# Router Decision

- Phase: reviewer
- Allowed: STOP_FAIL_SCOPE
- Selected: STOP_FAIL_SCOPE
- Fallback: yes (router disabled)

# Router Decision

- Phase: breakdown
- Allowed: PROVER, STOP_STALL
- Selected: PROVER
- Fallback: yes (router disabled)

# Router Decision

- Phase: prover
- Allowed: REVIEWER
- Selected: REVIEWER
- Fallback: yes (router disabled)

